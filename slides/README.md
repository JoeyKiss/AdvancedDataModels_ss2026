# Slides Directory

This folder holds the slide decks for each seminar session.  Student groups should place their presentation files here (e.g., `.pptx` or `.pdf`).  Name your file clearly (e.g., `graph_databases_group1.pptx`) and include any supplementary assets in a subfolder if needed.
